<view class="container">
  <view class="contactinfo">
    <button wx:if="{{!hasContactInfo && canIUse}}" open-type="getContactInfo" bindgetuserinfo="getContactInfo"> 保存收件人地址 </button>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
